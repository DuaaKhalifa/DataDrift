databaseChangeLog:
  - changeSet:
      id: "006"
      author: sarah.jones
      comment: Add check constraint for positive price
      changes:
        - sql:
            sql: |
              ALTER TABLE products
              ADD CONSTRAINT chk_products_price_positive
              CHECK (price > 0);
      rollback:
        - sql:
            sql: |
              ALTER TABLE products
              DROP CONSTRAINT IF EXISTS chk_products_price_positive;